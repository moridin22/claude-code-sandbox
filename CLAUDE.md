# Project Guidelines

## Version Bumping

Always update the `version` field in `package.json` with every push — this is the single source of truth and is displayed live on the webpage. Use semantic versioning:
- Patch bump (`0.0.x`) for bug fixes and small tweaks
- Minor bump (`0.x.0`) for new features
